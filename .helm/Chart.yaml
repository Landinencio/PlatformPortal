apiVersion: v2
name: platformportal
description: Platform Portal (Next.js) — umbrella chart over the corporate generic-chart
type: application
version: 1.0.0
appVersion: "1.0.0"
dependencies:
  - name: generic-chart
    # Corporate chart, published to the GitLab Package Registry (Helm repo).
    # >=0.5.0: native ESO (secret_manager.*) + CronJob (cronjobs.*) + ingress fixes
    # (drop removed common.ingress.supports* helpers; backend serviceName aligned).
    version: ">=0.5.0"
    repository: "https://gitlab.com/api/v4/projects/71265300/packages/helm/stable"
maintainers:
  - name: SRE / Platform Engineering
    email: ruben.landin@iskaypet.com
